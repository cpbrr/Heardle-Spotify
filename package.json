{
  "name": "heardle-spotify-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "A Vercel-hosted Spotify Heardle app.",
  "scripts": {
    "check": "node --check api/_spotify.js && node --check api/status.js && node --check api/login.js && node --check api/callback.js && node --check api/token.js && node --check api/logout.js"
  },
  "engines": {
    "node": ">=18"
  }
}
